<?xml version="1.0" encoding="utf-8"?>
<!-- Layout cho màn hình hiển thị kết quả quiz -->
<ScrollView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Result Icon -->
        <ImageView
            android:id="@+id/ivResultIcon"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginTop="32dp"
            android:src="@android:drawable/star_big_on"
            android:contentDescription="Kết quả"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Congratulations text -->
        <TextView
            android:id="@+id/tvCongratulations"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Chúc mừng!"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            app:layout_constraintTop_toBottomOf="@id/ivResultIcon"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Message based on score -->
        <TextView
            android:id="@+id/tvMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textSize="18sp"
            android:textColor="@android:color/darker_gray"
            tools:text="Xuất sắc! 🎉"
            app:layout_constraintTop_toBottomOf="@id/tvCongratulations"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Score Card -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardScore"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            app:cardElevation="4dp"
            app:cardCornerRadius="12dp"
            app:cardBackgroundColor="@color/primary"
            app:layout_constraintTop_toBottomOf="@id/tvMessage">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="24dp"
                android:gravity="center">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Điểm của bạn"
                    android:textSize="16sp"
                    android:textColor="@color/white"
                    android:alpha="0.9" />

                <!-- Score percentage -->
                <TextView
                    android:id="@+id/tvScorePercentage"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:textSize="48sp"
                    android:textStyle="bold"
                    android:textColor="@color/white"
                    tools:text="85%" />

                <!-- Correct answers -->
                <TextView
                    android:id="@+id/tvCorrectAnswers"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:textSize="16sp"
                    android:textColor="@color/white"
                    android:alpha="0.9"
                    tools:text="Trả lời đúng: 4/5" />

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

GROQ_API_KEY=your_groq_api_key_here        <!-- View Transcript Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnViewTranscript"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Xem bản ghi"
            android:textColor="@color/primary"
            app:strokeColor="@color/primary"
            app:icon="@android:drawable/ic_menu_view"
            app:iconTint="@color/primary"
            app:layout_constraintTop_toBottomOf="@id/cardScore" />

        <!-- Try Again Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnTryAgain"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Thử lại"
            app:backgroundTint="@color/primary"
            app:icon="@android:drawable/ic_menu_rotate"
            app:iconTint="@color/white"
            app:layout_constraintTop_toBottomOf="@id/btnViewTranscript" />

        <!-- Back to Lessons Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnBackToLessons"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Quay lại danh sách"
            android:textColor="@android:color/darker_gray"
            app:layout_constraintTop_toBottomOf="@id/btnTryAgain" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</ScrollView>
